@model IEnumerable<KendiDukkanim.Models.Siparis>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Urun)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Musteri)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Urun.UrunID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Musteri.FirstMidName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SiparisID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SiparisID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SiparisID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
