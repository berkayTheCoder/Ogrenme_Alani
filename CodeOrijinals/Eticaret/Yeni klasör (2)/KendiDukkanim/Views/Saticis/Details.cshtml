@model KendiDukkanim.Models.Satici
@inject KendiDukkanimContext context

@{
    ViewData["Title"] = "Details";
    var satislar = Model.SatisLari.ToList();
    //var satis = Convert(context.Satis.ToString());
}

<h1>Details</h1>

<div>
    <h4>Satici</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.LastName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.LastName)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.FirstMidName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.FirstMidName)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.ID">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
@Model.SatisLari;
@context.Satici.ToList();
@satislar;
@foreach (var item in satislar)
{
    <p>@item.Urun</p>
    @*<p>@satis</p>*@
}